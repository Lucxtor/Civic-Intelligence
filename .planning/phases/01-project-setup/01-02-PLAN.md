---
wave: 2
depends_on: 01-01
files_modified: [tailwind.config.ts, src/app/globals.css, src/app/layout.tsx, src/components/ui/LayoutWrapper.tsx]
autonomous: true
---

# Plan 01-02: Configure Global Layout and Design Variables

## Goal
Establish the "Floripa Web3 Techno-Optimist" design system theme natively in Tailwind and CSS with an Inter/Space Grotesk typography stack and deep dark mode aesthetics.

## Must Haves
- Tailwind configuration includes custom color palettes (Electric Yellow, Deep Magenta, Vibrant Green, Charcoal) and typography fonts.
- CSS file exposes glassmorphism utility classes.
- Root layout wraps the application properly.

## Requirements
- ARCH-02

## Tasks

<task>
<read_first>src/app/globals.css</read_first>
<action>
Update `src/app/globals.css` to:
1. Import `Inter` and `Space Grotesk` fonts via Google Fonts `@import`.
2. Redefine the `:root` and `.dark` CSS variables to enforce a native Dark Mode (with Charcoal backgrounds e.g., `#121212`, card backgrounds `#1A1A1A`).
3. Add a `.glass-panel` utility class that applies `backdrop-filter: blur(16px)`, `background: rgba(255, 255, 255, 0.05)`, and a subtle `border: 1px solid rgba(255, 255, 255, 0.1)`.
Force `.dark` theme globally by default on the body.
</action>
<acceptance_criteria>
- `src/app/globals.css` contains `@import url` for fonts.
- Class `.glass-panel` is defined.
- CSS variables for `--background` match charcoal darkness.
</acceptance_criteria>
</task>

<task>
<read_first>tailwind.config.ts</read_first>
<action>
Modify `tailwind.config.ts` to extend the theme:
- Add `fontFamily` for `sans` (Inter) and `heading` (Space Grotesk).
- Add custom colors: `ipeYellow` (#E1FF00), `ipeMagenta` (#FF00AA), `ipeGreen` (#00FF66).
</action>
<acceptance_criteria>
- `tailwind.config.ts` contains `ipeYellow`, `ipeMagenta`, `ipeGreen`.
- `tailwind.config.ts` has `fontFamily` mapped to Inter/Space Grotesk.
</acceptance_criteria>
</task>

<task>
<read_first>src/app/layout.tsx</read_first>
<action>
Update `src/app/layout.tsx` to:
1. Apply the fonts to the `className` of the `body`.
2. Add a persistent top navigation bar (logo "Ipê Civic" with Space Grotesk).
3. Ensure the background color is strictly applied to avoid flashes.
</action>
<acceptance_criteria>
- `src/app/layout.tsx` outputs a `header` or nav element with "Ipê Civic".
- `Inter` and `Space Grotesk` font configs are implemented (next/font/google or via global css).
</acceptance_criteria>
</task>

## Verification
- Running application successfully renders the "Ipê Civic" header in Space Grotesk against a dark background.
