---
wave: 1
depends_on: 
files_modified: [package.json, src/app/layout.tsx, src/app/globals.css]
autonomous: true
---

# Plan 01-01: Initialize Next.js, Tailwind, Shadcn

## Goal
Scaffold the foundational Next.js 14+ App Router project with Tailwind CSS and Shadcn UI.

## Must Haves
- Next.js server starts successfully
- Tailwind CSS is correctly resolving classes

## Requirements
- ARCH-01

## Tasks

<task>
<read_first>None (Greenfield)</read_first>
<action>
Execute `npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm` in the current directory `.` to scaffold the Next.js project. Answer 'Yes' to any prompts or pass `-y`. Since git is already initialized, it might not initialize git.
</action>
<acceptance_criteria>
- `package.json` exists in the root directory.
- `src/app/layout.tsx` exists.
</acceptance_criteria>
</task>

<task>
<read_first>package.json</read_first>
<action>
Initialize Shadcn UI by running `npx -y shadcn@latest init -d`. This installs standard configurations for Tailwind + CSS variables.
</action>
<acceptance_criteria>
- `components.json` exists in the root directory.
- `src/lib/utils.ts` exists.
</acceptance_criteria>
</task>

<task>
<read_first>package.json</read_first>
<action>
Install core runtime dependencies: `npm install framer-motion zustand lucide-react`.
</action>
<acceptance_criteria>
- `package.json` includes `framer-motion` and `zustand` in the `dependencies` block.
</acceptance_criteria>
</task>

## Verification
- Local dev server can be started without errors via `npm run dev`.
