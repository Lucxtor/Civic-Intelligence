---
wave: 2
depends_on: 01-01
files_modified: [src/store/useMockStore.ts, src/types/index.ts, src/components/motion/MotionBentoCard.tsx, src/components/motion/FadeInPage.tsx]
autonomous: true
---

# Plan 01-03: Mock Data Engine and Motion Core

## Goal
Implement the Zustand global state store for the mock data engine and establish the reusable Framer Motion layout wrappers (bento-box motion).

## Must Haves
- Zustand store manages an array of proposals and user votes.
- Framer Motion wrappers exist to easily wrap UI sections safely.

## Requirements
- ARCH-03, ARCH-04

## Tasks

<task>
<read_first>src/types/index.ts</read_first>
<action>
Create `src/types/index.ts` to define the TypeScript interfaces for the mock platform:
```typescript
export interface Proposal {
  id: string;
  title: string;
  abstract: string;
  eli5: string;
  impact: { helps: string[]; hurts: string[]; costs: string };
}
```
Define the `UserVote` type as well.
</action>
<acceptance_criteria>
- `src/types/index.ts` exposes `Proposal` and `UserVote`.
</acceptance_criteria>
</task>

<task>
<read_first>src/store/useMockStore.ts</read_first>
<action>
Create `src/store/useMockStore.ts` using `zustand`. 
Initialize state with 3 mock proposals.
Provide actions to `setVote(proposalId, voteData)`.
</action>
<acceptance_criteria>
- `src/store/useMockStore.ts` exports default `useMockStore` hook.
- Store initializes with at least 1 mock proposal in the array.
</acceptance_criteria>
</task>

<task>
<read_first>src/components/motion/MotionBentoCard.tsx, src/components/motion/FadeInPage.tsx</read_first>
<action>
Create reusable Framer Motion HOCs:
1. `src/components/motion/MotionBentoCard.tsx` (exports `MotionBentoCard` rendering a `motion.div` with an entrance spring animation and hover scaling).
2. `src/components/motion/FadeInPage.tsx` (exports `FadeInPage` rendering a `motion.div` wrapping children that fades in on load).
</action>
<acceptance_criteria>
- `MotionBentoCard.tsx` imports `framer-motion` and renders `motion.div`.
- `FadeInPage.tsx` imports `framer-motion` and renders `motion.div`.
</acceptance_criteria>
</task>

## Verification
- Running a typecheck using `tsc --noEmit` passes cleanly.
