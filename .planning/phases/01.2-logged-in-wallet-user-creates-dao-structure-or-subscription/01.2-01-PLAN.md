---
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
autonomous: true
---

# Plan: Subscription Plans on Landing Page

## Goal
Add the subscription pricing tiers to the landing page as an entry point for DAOs to create custom Civic Intelligence instances. Subdomain routing logic is explicitly deferred.

<read_first>
- src/app/page.tsx
- .planning/phases/01.2-logged-in-wallet-user-creates-dao-structure-or-subscription/01.2-CONTEXT.md
</read_first>

## Tasks

<task>
<action>
Add a new pricing tier section to `src/app/page.tsx`. Insert it between the Live Activity Strip (or the Pillars section if no active proposals exist) and the Footer CTA.

The section should feature a container with a heading like "Launch Your Own Civic Intelligence Platform" and three pricing tiers:
1. **Citizen (Free)**: Access global public proposals and vote.
2. **DAO Forge**: Create a custom subdomain (e.g. `ipe.civic.app`), brand colors, and isolated proposals for a monthly fee.
3. **Ecosystem**: Full enterprise multi-tenant cluster.

Use `framer-motion` for spring-loaded staggered entry (`fadeUp` / `stagger` variants already present in the file), and format the cards as `glass-panel` components. Highlight the "DAO Forge" middle tier prominently with an `ipe-green` border and a solid button.
</action>
<read_first>
- src/app/page.tsx
</read_first>
<acceptance_criteria>
- src/app/page.tsx contains a section with "Citizen", "DAO Forge", and "Ecosystem" tiers.
- src/app/page.tsx uses `glass-panel` classes for the tier cards.
</acceptance_criteria>
</task>

## Verification
- Run the Next.js dev server.
- Verify the pricing cards render correctly on the homepage, below the pillars/live-activity strips.
- Verify responsive layout works for smaller viewports.

## Must Haves
- The DAO Forge tier must be visually distinguished pointing to the capability requested (launching own instance).
- Action buttons should exist (e.g., clicking them simply redirects to `#` or `/admin` for now).
